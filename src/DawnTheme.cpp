#include "DawnTheme.h"

// Dawn::Theme is a plain aggregate of compile-time defaults declared in the header.
// This translation unit exists so the theme has a home in the build (and the brief's
// source layout), and as the place any future computed-default helpers would live.
namespace Dawn {

// (intentionally empty for the PoC)

} // namespace Dawn
